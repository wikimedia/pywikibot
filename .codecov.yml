codecov:
  bot: jayvdb

coverage:
  status:
    project:
      default:
        enabled: yes
    patch:
      default:
        enabled: yes
    changes:
      default:
        enabled: yes

  ignore:
    - ez_setup.py
    - tests/*edit*.py
    - tests/*upload*.py

  notify:
    irc:
      default:
        server: chat.freenode.net
        channel: pywikibot

    gitter:
      default:
        url: https://webhooks.gitter.im/e/f312b840da1c40d9f4be
